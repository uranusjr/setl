# Stub so we can run `python setup.py` commands during development. In time we
# want to make setl be able to self-build and eliminating this file altogether.

# NOTE: DO NOT ADD ANYTHING HERE; use setup.cfg instead.

from setuptools import setup

setup()
